#!/usr/bin/env ruby
# CLI entry point for Headway. Prints version on execution.
# Extended in Task 9 with run and version commands.

require_relative "../lib/headway"

puts "Headway v#{Headway::VERSION}"
