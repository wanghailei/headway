#!/usr/bin/env ruby
# CLI entry point for Headway. Loads Bundler, dotenv, and dispatches
# to the Thor-based CLI.

require "bundler/setup"
require "dotenv/load"

$LOAD_PATH.unshift File.expand_path( "../lib", __dir__ )
require "headway"

Headway::CLI.start( ARGV )
